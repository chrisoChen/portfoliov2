import { Container } from "@mui/system";
import { Typography } from "@mui/material";

function Experience() {
  return (
    <Container variant="sectionContainer" sx={{ textAlign: "center" }}>
      <Typography variant="h2">
        
      </Typography>
    </Container>
  );
}

export default Experience;
