import iconReducer from "../util/iconReducer";

beforeAll(() => {
  const links = { github: "https://jestjs.io/docs/setup-teardown" };
});

describe("links", () => {
  test("reducer renders logo icons", () => {});
});
